// Shared by SQL Practice and Data Analytics — both tracks use the same four
// difficulty levels, so this lives independently instead of one track's
// question data importing from the other's.
export type Difficulty = 'Easy' | 'Medium' | 'Hard' | 'Interview'
