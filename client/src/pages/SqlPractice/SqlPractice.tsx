import PlaceholderSection from '../../components/PlaceholderSection/PlaceholderSection'

function SqlPractice() {
  return (
    <PlaceholderSection
      title="SQL Practice"
      description="Easy, Medium, Hard, and Interview questions across joins, CASE WHEN, string/date functions, window functions, views, indexes, and query optimization."
      upcomingPhase="Built in Phase 1 — in-browser SQLite engine (sql.js) with seeded datasets."
    />
  )
}

export default SqlPractice
